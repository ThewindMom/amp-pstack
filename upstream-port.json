{
	"upstream": {
		"repository": "https://github.com/cursor/plugins",
		"path": "pstack",
		"commit": "12d587dfb20741cafc376c42c696c5f6e2a64487",
		"version": "0.15.5",
		"tree": "https://github.com/cursor/plugins/tree/12d587dfb20741cafc376c42c696c5f6e2a64487/pstack"
	},
	"intentionalDepartures": [
		{
			"id": "subscription-cli-backends",
			"from": "Cursor Task runs configured model seats, with same-family retry for rejected models.",
			"to": "Grok workers use Cursor CLI unless the active SuperGrok subscription wins native routing; Opus defaults to Claude Code. Each run selects one backend, reports failure without retry, isolates its snapshot, and returns writer patches for parent review.",
			"why": "The user requested subscription CLI routing and then approved dropping automatic failover. Amp's advertised plugin and native creation APIs do not expose external-agent model selection and terminal lifecycle contracts."
		},
		{
			"id": "guide-condensation",
			"from": "Cursor's guide repeats workflows through longer examples and editor-specific instructions.",
			"to": "The Amp guide condenses examples and links to authoritative skills and playbooks, retaining task switching, session pickup, design choices, verification, overnight audit, and skill evaluation contracts.",
			"why": "Editorial condensation avoids repeating platform-specific instructions without changing the workflows."
		},
		{
			"id": "benny-removed",
			"from": "Upstream bundles a Slack issue-triage and reproduction automation pack.",
			"to": "The automation pack is omitted; generic wake webhooks remain available.",
			"why": "The user requested removal of the unused automation."
		},
		{
			"id": "amp-host-autonomy",
			"from": "**Just do it.** Use any MCP tool. Reversible work and external actions proceed without asking. Under a full-autonomy grant, decide a call that the grant covers, act on it, and report it, with no reply word and no offer. Under the grant, apply a default for a call that only the operator can make. Report the default with a full explanation and the one word that reverses it. Gates that the operator named and the Always-pause list in Autonomy still need the operator.",
			"to": "Amp preserves those full-autonomy decision boundaries, but full autonomy does not authorize shared or external writes; those still require the user's explicit approval.",
			"why": "Amp permission boundaries are stricter than Cursor pstack autonomy."
		},
		{
			"id": "durable-threads",
			"from": "**Defaults for every `Task` call.** `run_in_background: true`",
			"to": "Amp child threads with durable IDs, orbs, named runners, and schedules. Details live in references/amp-adapter.md.",
			"why": "Amp has no Cursor Task. Durable threads are the native equivalent."
		},
		{
			"id": "runtime-mode-advisory",
			"from": "Cursor custom mode with disable-model-invocation: true",
			"to": "Amp poteto extends builtin medium. Mode instructions are advisory and do not strip write tools or invent tool gates.",
			"why": "Amp plugin modes cannot sandbox the coordinator or enforce the playbook."
		},
		{
			"id": "parent-medium",
			"from": "Cursor parent uses the user's chat model",
			"to": "Amp poteto extends builtin medium, pins openai/gpt-6-sol at medium reasoning, and persists the full Poteto skill in the mode instructions.",
			"why": "The user requested GPT-6 Sol medium and Cursor Use as Mode parity. Amp's runtime catalog confirms this model and effort."
		},
		{
			"id": "feature-nested-spawn-sentence",
			"from": "You can spawn a subagent even though you are one. \"The app is small\" and \"a subagent cannot spawn one\" are both wrong.",
			"to": "Feature keeps the Amp sentence that a child can spawn a child, and that a small app is not a reason to skip delegation.",
			"why": "0.15.5 removed that Cursor sentence. Amp still needs it so an owner does not treat nested spawn as impossible."
		},
		{
			"id": "check-plan-nonblank-model",
			"from": "Ten lanes on `[^`<>]+` at the PR head",
			"to": "The Amp checker also rejects a whitespace-only model inside the backticks.",
			"why": "Upstream 0.15.5 rejects angle brackets and empty ticks. A blank model is not a filled model, so the Amp checker tightens that boundary."
		},
		{
			"id": "no-cursor-model-aliases",
			"from": "A role line of inherit-parent or auto omits Task model. A rejected slug falls back to the same family on that Task call.",
			"to": "Amp roles resolve through the plugin. inherit-parent and auto stay invalid. A rejected single-role spawn stops. A rejected panel seat is one dropout. A replacement config write happens only when the user asks, then reload before the next orb spawn. There is no per-call model argument.",
			"why": "pstack_start_agent and pstack_run_panel take a role or panel. modelFor and panelFor resolve the map. Orb agents are registered at plugin load."
		},
		{
			"id": "catalog-disable-model-invocation",
			"from": "Cursor marks routed pstack skills disable-model-invocation: true.",
			"to": "Amp keeps the skills in its catalog but scopes their descriptions to named use or poteto-mode routing; setup-pstack remains directly discoverable.",
			"why": "Amp skill frontmatter has no equivalent catalog suppression flag, so trigger wording carries the routing boundary."
		},
		{
			"id": "amp-seat-effort-budget",
			"from": "Cursor budgets rewrite effort-bearing model slugs and allow inherit-parent or auto.",
			"to": "Amp setup reads show-agent-options capabilities and writes per-seat model and effort values, capping each concrete model to supported effort while mapping builtin budgets to builtin aliases.",
			"why": "Amp exposes effort separately from model IDs and validates per-model effort capabilities at runtime."
		},
		{
			"id": "amp-panel-count",
			"from": "Custom arena counts launch one Task per candidate model choice.",
			"to": "Amp passes count, capped at 20, to one pstack_run_panel call and cycles configured seats in order.",
			"why": "Amp panels own candidate labeling, run tracking, dropout handling, and cross-judge gating; arbitrary per-candidate roles do not exist."
		},
		{
			"id": "synced-script-invocation",
			"from": "Cursor instructions invoke scripts relative to the plugin checkout and may execute shell scripts directly.",
			"to": "Amp instructions resolve scripts from the loaded skill base and invoke them explicitly with bun or bash.",
			"why": "A synced Amp install may run from any user working directory and does not preserve executable bits."
		}
	]
}
